# Root .env.example for the entire project
# Copy this file and create separate .env files:
# - backend/.env (for Django backend)
# - frontend/.env.local (for Next.js frontend)

# See backend/.env.example and frontend/.env.example for specific configurations

# PostgreSQL Database (Docker Compose)
POSTGRES_DB=imdb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
